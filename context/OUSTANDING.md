# Outstanding Questions

- [ ] PRD-002: Should `asm add` support non-git remote archives?
- [ ] PRD-003: Add `--strict` to fail on sync/cleanup warnings.
- [ ] PROJ-001: `asm add` should auto-sync after install.
